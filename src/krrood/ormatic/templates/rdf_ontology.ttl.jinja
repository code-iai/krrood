# Generated by ORMatic RDF Generator
# Ontology URI: {{ namespace }}

@prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .
@prefix : <{{ namespace }}> .

<{{ namespace }}> a owl:Ontology ;
    rdfs:label "Generated Ontology" ;
    rdfs:comment "Automatically generated from Python dataclasses using ORMatic" .

#################################################################
#    Classes
#################################################################

{% for ontology in ontologies %}
### {{ namespace }}{{ ontology.class_name }}
:{{ ontology.class_name }} a owl:Class ;
    rdfs:label "{{ ontology.class_name }}" {% if ontology.parent_classes %}; {% endif %}

{% for parent in ontology.parent_classes %}
    rdfs:subClassOf :{{ parent }}{% if not loop.last %} ,{% else %} .{% endif %}

{% endfor %}
{% if not ontology.parent_classes %}.{% endif %}

{% endfor %}

#################################################################
#    Datatype Properties
#################################################################

{% for ontology in ontologies %}
{% for prop in ontology.datatype_properties %}
### {{ namespace }}{{ prop.name }}
:{{ prop.name }} a owl:DatatypeProperty ;
    rdfs:label "{{ prop.name }}" ;
    rdfs:domain :{{ prop.domain }} ;
    rdfs:range {{ prop.range }} .

{% endfor %}
{% endfor %}

#################################################################
#    Object Properties
#################################################################

{% for ontology in ontologies %}
{% for prop in ontology.object_properties %}
### {{ namespace }}{{ prop.name }}
:{{ prop.name }} a owl:ObjectProperty ;
    rdfs:label "{{ prop.name }}" ;
    rdfs:domain :{{ prop.domain }} ;
    rdfs:range :{{ prop.range }} .

{% endfor %}
{% endfor %}

###  Generated by ORMatic RDF Generator
